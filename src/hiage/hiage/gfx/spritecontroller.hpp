#pragma once

#include "renderer.h"
#include "sprite.h"
#include "../util/vector2.h"

namespace hiage 
{
    /*
    Class for doing everything related to sprites -- rendering, controlling animations, etc.
    This way, the sprite object itself can remain a pure data object.
    */
    class SpriteController
    {
    public:
        //SpriteController(Game& game)
        //void render(const Sprite& sprite, Renderer &renderer, const Vector2<double>& position, ObjectZ z, float rotation = 0, bool hFlip = false, bool vFlip = false, float maxSize = 0) const;
    }
}